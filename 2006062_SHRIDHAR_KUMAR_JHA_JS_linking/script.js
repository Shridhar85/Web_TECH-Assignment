document.body.innerHTML='<h2>Linking javscript file to  html page using<span> external linking</span>.we can link html files to javascript file using external and internal linking. In external linking we make an external javaScript file and link it with the html file. In internal linking we write our javascript code inside that html file under script tag.<h2>';

